ALTER TABLE "conversations"
ADD COLUMN IF NOT EXISTS "threadUrl" TEXT,
ADD COLUMN IF NOT EXISTS "syncState" TEXT NOT NULL DEFAULT 'shell_only',
ADD COLUMN IF NOT EXISTS "messageCount" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "lastResolvedAt" TIMESTAMP(3),
ADD COLUMN IF NOT EXISTS "resolveAttempts" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "resolveError" TEXT;
